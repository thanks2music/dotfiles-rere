# 綺麗な4分割用コマンド

# 右上
split-window -h -t 0 -c '#{pane_current_path}'

# 左下
split-window -v -t 0 -c '#{pane_current_path}'

# 右下
split-window -h -t 2 -c '#{pane_current_path}'

# 最後にきれいに整列
select-layout tiled
select-pane -t 0
